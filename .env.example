# Copy to .env for local dev, and set the same variables in your Netlify site
# (Site settings > Environment variables). See SETUP-VERIFICATION.md.

# Supabase project: Dashboard > Project Settings > API
PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Gated Discord invite. Server-only secret, never exposed to the client bundle.
DISCORD_INVITE_URL=https://discord.gg/your-invite
