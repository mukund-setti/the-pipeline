---
import Layout from '../components/Layout.astro';
import Eyebrow from '../components/ui/Eyebrow.astro';
import Conduit from '../components/ui/Conduit.astro';
import Button from '../components/ui/Button.astro';
import SectionHead from '../components/ui/SectionHead.astro';
import StatGrid from '../components/ui/StatGrid.astro';
import StageCard from '../components/ui/StageCard.astro';
import CtaBand from '../components/ui/CtaBand.astro';
import DropFeed from '../components/ui/DropFeed.tsx';
import ScrollReveal from '../components/ui/ScrollReveal.tsx';
import { site, hero, placements, stages } from '../data/site';
---
<Layout
  title="The Pipeline · A fellow-run community across the UC system"
  description={site.description}
  ogTitle="The Pipeline · Across the UC system"
  ogDescription={site.ogDescription}
>
  {/* ── Hero: under the canopy ───────────────────────────── */}
  <section class="on-ink relative overflow-hidden bg-canopy pb-24 pt-[88px] text-ondark max-[560px]:pb-16 max-[560px]:pt-14">
    <div aria-hidden="true" class="pointer-events-none absolute inset-0">
      <img
        src="/img/hero-students.webp"
        alt=""
        width="1800"
        height="1208"
        decoding="async"
        fetchpriority="low"
        class="absolute inset-y-0 right-0 h-full w-full object-cover object-center opacity-15 md:w-[62%] md:opacity-40"
      />
      <div class="photo-wash-right absolute inset-0 hidden md:block"></div>
      <div class="absolute inset-0 bg-canopy/80 md:hidden"></div>
      <div class="light-shaft"></div>
    </div>

    <div class="wrap relative">
      <Eyebrow tone="onDark" class="animate-riseIn flex" as="div">{hero.eyebrow}</Eyebrow>
      <h1
        class="animate-riseIn mb-[26px] mt-[26px] max-w-[17ch] font-display text-hero font-semibold tracking-tight text-ondark"
        style="animation-delay:80ms"
      >
        We don't fix the pipeline. We <span class="hl-gold">are</span> the pipeline.
      </h1>
      <p
        class="animate-riseIn mb-[34px] max-w-[54ch] text-lede text-ondark-soft [&_b]:font-semibold [&_b]:text-ondark"
        style="animation-delay:160ms"
      >
        A fellow-run community across the UC system moving underrepresented <b>undergrads in tech</b>
        into the rooms they were kept out of, through <b>gatekept job drops</b>, <b>resume reviews</b>,
        and <b>mock interviews</b> run by people who have already landed.
      </p>
      <div class="animate-riseIn flex flex-wrap items-center gap-3" style="animation-delay:240ms">
        <Button href="/join/" arrow>Join the Pipeline</Button>
        <Button href="/how/" variant="ghostDark">See how it works</Button>
      </div>
      <div
        class="label animate-riseIn mt-8 inline-flex items-center gap-2.5 rounded-pill border border-line-dark bg-canopy-2/70 px-4 py-2.5 text-[0.68rem] text-ondark-soft"
        style="animation-delay:300ms"
      >
        <span class="h-2 w-2 flex-none rounded-full bg-gold" aria-hidden="true"></span>
        {hero.proof}
      </div>
      <div class="animate-riseIn mt-16" style="animation-delay:360ms">
        <Conduit tone="dark" notes />
      </div>
    </div>
  </section>

  {/* ── Drop feed ────────────────────────────────────────── */}
  <section class="on-ink relative overflow-hidden border-y border-line-dark bg-canopy-2 py-12 text-ondark">
    <div aria-hidden="true" class="light-shaft-soft"></div>
    <div class="wrap relative">
      <div class="mb-6 flex items-end justify-between gap-4">
        <Eyebrow tone="onDark">Where our fellows have landed</Eyebrow>
        <a
          href="/outcomes/"
          class="label hidden text-[0.7rem] text-gold transition-colors hover:text-gold-pale md:inline"
        >
          See all outcomes &rarr;
        </a>
      </div>
    </div>
    <DropFeed placements={placements} client:visible />
    <div class="wrap relative">
      <div class="mt-6 md:hidden">
        <a href="/outcomes/" class="label text-[0.7rem] text-gold">See all outcomes &rarr;</a>
      </div>
    </div>
  </section>

  {/* ── Stats ────────────────────────────────────────────── */}
  <section class="py-20 md:py-24">
    <div class="wrap">
      <ScrollReveal client:visible>
        <StatGrid />
      </ScrollReveal>
    </div>
  </section>

  {/* ── How it works (teaser) ────────────────────────────── */}
  <section class="pb-20 md:pb-28">
    <div class="wrap">
      <SectionHead eyebrow="How it works" title="Three stages, one direction: forward." class="mb-12" />
      <ScrollReveal
        stagger
        client:visible
        className="grid grid-cols-3 gap-5 max-[860px]:grid-cols-2 max-[560px]:grid-cols-1"
      >
        {
          stages.map((s) => (
            <StageCard num={s.num} label={s.label} title={s.title} body={s.short} />
          ))
        }
      </ScrollReveal>
      <div class="mt-10">
        <Button href="/how/" variant="ghost" arrow>See what every fellow gets</Button>
      </div>
    </div>
  </section>

  {/* ── Mission (teaser) ─────────────────────────────────── */}
  <section class="on-ink relative overflow-hidden bg-canopy py-24 text-ondark">
    <img
      src="/img/mission-students.webp"
      alt=""
      aria-hidden="true"
      loading="lazy"
      decoding="async"
      width="1600"
      height="1104"
      class="absolute inset-0 h-full w-full object-cover opacity-15"
    />
    <div aria-hidden="true" class="photo-wash-ink absolute inset-0"></div>
    <div aria-hidden="true" class="light-shaft"></div>
    <div class="wrap relative">
      <ScrollReveal client:visible>
        <div class="mx-auto max-w-3xl text-center">
          <Eyebrow tone="onDark" class="justify-center">Why we exist</Eyebrow>
          <h2
            class="mx-auto mt-5 max-w-[20ch] font-display text-[clamp(2rem,5vw,3.4rem)] font-semibold leading-[1.08] tracking-tight text-ondark"
          >
            The talent was <span class="hl-gold">never</span> the problem.
          </h2>
          <p
            class="mx-auto mt-6 max-w-[58ch] text-[clamp(1.05rem,1.7vw,1.3rem)] leading-relaxed text-ondark-soft [&_b]:font-semibold [&_b]:text-ondark"
          >
            People call it the "pipeline problem", as if the talent is not out there. It is. What is
            gatekept is <b>access</b>. The Pipeline hands that access to the people who have been
            locked out of it, and lets the work speak for itself.
          </p>
          <div class="mt-8 flex justify-center">
            <Button href="/about/" variant="ghostDark" arrow>Read the whole story</Button>
          </div>
        </div>
      </ScrollReveal>
    </div>
  </section>

  {/* ── Join CTA ─────────────────────────────────────────── */}
  <section class="py-20">
    <div class="wrap">
      <CtaBand eyebrow="Join" title="Get in the pipeline.">
        <Fragment slot="body">
          You don't need a connection to get in here. You need a UC email and two minutes.
          <b>The access is the part we hand you.</b>
        </Fragment>
        <Fragment slot="actions">
          <Button href="/join/" arrow>How to join</Button>
          <Button href="/join/" variant="ghostDark">Join the Discord</Button>
        </Fragment>
      </CtaBand>
    </div>
  </section>
</Layout>
